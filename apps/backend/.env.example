# Backend Environment Variables

# Database Configuration
# Lokale Entwicklung (Docker Compose verwendet Port 5433)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/budget-tracker?schema=public&sslmode=disable

# Direkter Database-Zugriff (für Prisma Migrations & Studio)
# Für Neon oder andere managed PostgreSQL-DBs:
# DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
# DIRECT_DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
DIRECT_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/budget-tracker?schema=public&sslmode=disable

# API Server Configuration
PORT_API=3001

# CORS Origins (kommagetrennt für mehrere Origins)
CORS_ORIGIN=http://localhost:4201

# JWT Secrets (WICHTIG: In Produktion durch sichere, zufällige Werte ersetzen!)
# Generiere sichere Secrets mit: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production

# Node Environment
NODE_ENV=development
