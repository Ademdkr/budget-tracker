# Backend Environment Variables - NEON PRODUCTION
# ⚠️ WICHTIG: Diese Datei enthält die Neon Production Connection
# Umbenennen zu .env um diese Verbindung zu verwenden
# Backup der lokalen .env vor dem Überschreiben empfohlen!

# Neon PostgreSQL Production Database
# HINWEIS: Ersetze diese Werte mit deinen tatsächlichen Neon Connection Strings
# Diese müssen aus deinem Neon Dashboard kopiert werden
DATABASE_URL=postgresql://neondb_owner:npg_Zfvt1xn3rFCP@ep-holy-cake-agz4x04m-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
DIRECT_DATABASE_URL=postgresql://neondb_owner:npg_Zfvt1xn3rFCP@ep-holy-cake-agz4x04m.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# API Server
PORT_API=3001
CORS_ORIGIN=http://localhost:4201

# JWT Secrets (Production-Werte aus GitHub Secrets verwenden)
JWT_SECRET=dev-secret-change-in-production
JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production

# Node Environment
NODE_ENV=development

# 📝 So holst du deine Neon Connection Strings:
# 1. Gehe zu https://console.neon.tech
# 2. Wähle dein "budget-tracker-production" Projekt
# 3. Klicke auf "Connection Details"
# 4. Kopiere "Connection string" (pooled) für DATABASE_URL
# 5. Kopiere "Direct connection string" für DIRECT_DATABASE_URL
# 6. Ersetze die Werte oben

# 🔄 Zwischen lokaler und Neon DB wechseln:
# Lokal:  cp apps/backend/.env.local apps/backend/.env
# Neon:   cp apps/backend/.env.neon apps/backend/.env
