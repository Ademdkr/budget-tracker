# Worker Environment Variables
# Für lokale Entwicklung mit wrangler dev

# Umgebung
ENVIRONMENT=development

# Datenbank-URL (Neon oder andere PostgreSQL-kompatible DB)
# WICHTIG: In Produktion über 'wrangler secret put DATABASE_URL' setzen!
# Niemals direkt in .env für Produktion speichern
# DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# Lokale Entwicklung (falls Worker lokal auf lokale DB zugreift)
# DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/budget-tracker?schema=public&sslmode=disable
